######################################################################
# Include Generic ACE Macros
######################################################################
# Generic macros shared between all printer types are in ace_macros_generic.cfg
# Expected supporting macros (provided by printer/printer_generic_macros):
# - CUT_TIP
# - TO_THROW_POSITION
# - RESUME_NOZZLE_WIPE_SEQUENCE
# - FLUSH_POOP
# - _PAUSE_RESUME_STATE (optional; guarded)
[include ace_macros_generic.cfg]

######################################################################
# Custom Tool Macros (Optional - For Spoolman/Extensions)
######################################################################
# ACE automatically detects custom T<n> macros and skips auto-registration.
# Use this for integrations like Spoolman: https://github.com/Donkie/Spoolman
#
# Example: Custom T0 macro with Spoolman integration
[gcode_macro T0]
gcode:
    RESPOND MSG="=== T0 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t0 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=0
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=0

[gcode_macro T1]
gcode:
    RESPOND MSG="=== T1 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t1 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=1
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=1

[gcode_macro T2]
gcode:
    RESPOND MSG="=== T2 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t2 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=2
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=2
    
[gcode_macro T3]
gcode:
    RESPOND MSG="=== T3 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t3 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=3
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=3

[gcode_macro T4]
gcode:
    RESPOND MSG="=== T4 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t4 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=4
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=4
    
[gcode_macro T5]
gcode:
    RESPOND MSG="=== T5 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t5 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=5
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=5

[gcode_macro T6]
gcode:
    RESPOND MSG="=== T6 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t6 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=6
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=6

[gcode_macro T7]
gcode:
    RESPOND MSG="=== T7 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t7 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=7
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=7

[gcode_macro T8]
gcode:
    RESPOND MSG="=== T8 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t8 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=8
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=8

[gcode_macro T9]
gcode:
    RESPOND MSG="=== T9 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t9 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=9
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=9

[gcode_macro T10]
gcode:
    RESPOND MSG="=== T10 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t10 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=10
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=10

[gcode_macro T11]
gcode:
    RESPOND MSG="=== T11 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t11 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=11
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=11

[gcode_macro T12]
gcode:
    RESPOND MSG="=== T12 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t12 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=12
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=12

[gcode_macro T13]
gcode:
    RESPOND MSG="=== T13 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t13 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=13
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=13

[gcode_macro T14]
gcode:
    RESPOND MSG="=== T14 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t14 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=14
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=14

[gcode_macro T15]
gcode:
    RESPOND MSG="=== T15 MACRO START ==="
    RESPOND MSG="Raw params: {rawparams}"
    {% set raw = rawparams %}
    {% if 'PURGELENGTH=' in raw %}
        {% set purge_str = raw.split('PURGELENGTH=')[1].split(' ')[0] %}
        {% set PURGELENGTH = purge_str|float %}
    {% else %}
        {% set PURGELENGTH = 50.0 %}
    {% endif %}
    RESPOND MSG="Manually parsed PURGELENGTH: {PURGELENGTH}"
    {% set physical_gate = printer["gcode_macro _TOOL_MAP"].t15 %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=active VALUE=15
    ACE_SET_PURGE_AMOUNT PURGELENGTH={PURGELENGTH}
    ACE_CHANGE_TOOL TOOL={physical_gate} SLICER_TOOL=15
    
[gcode_macro T16]
description: Direct-feed external spool, bypasses ACE Pro
gcode:
    {% if printer.toolhead.extruder != 'extruder' %}
        ACTIVATE_EXTRUDER EXTRUDER=extruder
    {% endif %}
    M117 Direct feed - external spool
    # Do NOT call ACE_CHANGE_TOOL
######################################################################
# Please check that [save_variables] is above [ace] if you're using different config
[save_variables]
filename: ~/printer_data/config/saved_variables.cfg

# Workaround to get an on/off switch to toggle between ACE Pro and external spool printing
[virtual_pins]

[output_pin ACE_Pro]
pin: virtual_pin:ace_pro
pwm: False
value: 1
shutdown_value: 0

[respond]

[ace]
ace_count: 1
baud: 115200
#Mapping of filament runout sensors in ACE to printer sensors (K3 has no RDM sensor)
#filament_runout_sensor_name_rdm: filament_runout_rdm
filament_runout_sensor_name_nozzle: filament_runout_nozzle

# Default state of feed-assist after ACE connect/re-connect.
# This controls if ACE automatically starts pushing out filament of the last active slot after startup,
# unless it detects resistance via the rear buffer-filament sensors and stops.
feed_assist_active_after_ace_connect: True

# Connection supervision: monitors ACE connection stability and shows dialog/pauses print
# if connection becomes unstable (6+ reconnects in 3 minutes). Set to False to disable.
#ace_connection_supervision: True

# RFID temperature mode: how to calculate print temp from RFID tag min/max values
# Options: average (default), min, max
# Example: RFID tag with extruder_temp: {min: 190, max: 230}
#   average -> (190 + 230) / 2 = 210°C
#   min -> 190°C
#   max -> 230°C
#rfid_temp_mode: average

# Default feeding speed
feed_speed: 60
# Default retraction speed
retract_speed: 50

# Value must be greater than the tube length, as it will cause an error and pause the print if more than
# this amount is fed (and toolhead filament sensor has not triggered).
total_max_feeding_length: 3000

# Tube length between filament detection sensor on toolhead and ACE filament parking position
# (where filament is automatically fed by ACE when a new spool is inserted).
# This is the distance filament needs to be retracted to free the path for other filament.
# Should be slightly greater than actual tube length.
parkposition_to_toolhead_length: 115
parkposition_to_rdm_length: 0

# Length of filament (after extruder_feeding) to feed from toolhead extruder to nozzle, so it's primed to extrude.
toolhead_full_purge_length: 60
toolhead_slow_loading_speed: 6
toolhead_retraction_speed: 5
toolhead_retraction_length: 55
# Should be roughly the length between ACE and the printer's 4-in-1 splitter.
toolchange_load_length: 3000 

#max_dryer_temperature: 55

# Length and speed to feed from ACE and extrude after toolhead sensor has triggered
# (to ensure extruder gears have grabbed the filament securely).
extruder_feeding_length: 45
extruder_feeding_speed: 4


default_color_change_purge_length: 50
default_color_change_purge_speed: 300
purge_max_chunk_length: 250
purge_multiplier: 1.0
# How much filament to feed incrementally as a retry if initial loading failed
# (stops if total_max_feeding_length is reached).
incremental_feeding_length: 100 
incremental_feeding_speed: 60 

pre_cut_retract_length: 2  # Length to retract before CUT_TIP is called

